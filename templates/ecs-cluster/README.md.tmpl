# Stack `{{STACK_ID}}`

| Field | Value |
| --- | --- |
| Product | ecs-cluster |
| Environment | {{ENVIRONMENT}} |
| Control issue | {{CONTROL_REPO}}#{{ISSUE_NUMBER}} |
| Module | `{{MODULES_REPOSITORY}}?ref={{MODULE_REF}}` |
| Cluster | `{{CLUSTER_NAME}}` |

Provisioned via IssueOps. Plan/apply runs in this workload repo through `tofu-pipeline`.
