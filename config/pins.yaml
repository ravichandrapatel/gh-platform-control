# Immutable pins only. Floating refs (main/master/latest/HEAD) are rejected.
# Replace OWNER after remotes exist.
#
# After merging dual-runner changes to gh-platform-actions, bump actions.ref to the
# new 40-char SHA and update every infra-* tofu.yml / drift.yml uses:@ pin.

actions:
  repository: ravichandrapatel/gh-platform-actions
  # Immutable SHA on main that includes tofu-pipeline.
  # NOTE: bump after publishing actions dual-runner (tofu + terragrunt) commit.
  # Includes guard-new-stacks composite + tofu-pipeline + drift-reconcile.
  ref: 952412b3bffed728a9e07cd42cca705eda0a621f

modules:
  repository: ravichandrapatel/gh-platform-modules
  tagging_ref: tagging/v1.0.0

aws:
  region: us-east-1
