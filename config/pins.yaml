# Immutable pins only. Floating refs (main/master/latest/HEAD) are rejected.
# Replace OWNER after remotes exist.

actions:
  repository: ravichandrapatel/gh-platform-actions
  # Pin to tofu-pipeline branch tip (validation-only; replace with release SHA when ready).
  ref: 7ad28f6e7a7375f1ba6e78d6d9cd40f4eb0eba22

modules:
  repository: ravichandrapatel/gh-platform-modules
  # Default tagging module pin used by generated stacks (product catalog overrides path modules).
  tagging_ref: tagging/v0.0.0

aws:
  region: us-east-1
