# Example remote state for a workload stack (do not commit secrets).
# Prefer per-account buckets; key layout: stacks/<stack_id>/terraform.tfstate
bucket       = "tfstate-ACCOUNT_ID-dev"
key          = "stacks/example-stack/terraform.tfstate"
region       = "us-east-1"
encrypt      = true
use_lockfile = true
